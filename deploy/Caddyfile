# Set DOMAIN in .env (e.g. content.infrapilot.tech)
# Caddy obtains HTTPS certificates automatically.

{$DOMAIN} {
	reverse_proxy web:8000
}

# Optional: redirect www → apex (uncomment and set DOMAIN=yourdomain.com)
# www.{$DOMAIN} {
# 	redir https://{$DOMAIN}{uri} permanent
# }
